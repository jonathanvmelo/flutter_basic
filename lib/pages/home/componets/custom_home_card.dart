import 'package:flutter/material.dart';

class CustomHomeCard extends StatelessWidget {

 final Widget? child;
 final Color color;
 final List<Widget>? children;

 CustomHomeCard(this.child, this.color, {super.key, this.children});

 @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
